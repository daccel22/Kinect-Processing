/*
  Empty program with a timer.
  Can create a program whose animation changed with time/
*/

//if kinect is used, add the KinectPV2 library
void setup(){
  //do as the program need
}

void draw(){
  if(millis()<20000){//could be any number, change it as request change
    //do some animation
  }
  if(millis()>=20000&&millis()<40000){
    //do some animation
  }
  //......
}
